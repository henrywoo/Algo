#ifndef __ALGO_HASHTABLE__
#define __ALGO_HASHTABLE__

// chaining
template <class T>
class htable{
    int hash(int){
        int r;
        return r;
    }
    int hash1(){
        int r;
        return r;
    }
    int hash2(int ){
        int r;
        return r;
    }

public:
    bool set(const T& key, const T& val){
        return true;
    }
    T& get(const T& key){
        return key;
    }
};

// open-adressing

// Cuckoo hashing


#endif